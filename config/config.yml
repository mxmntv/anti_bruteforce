app:
  name: 'antibruteforce'
  version: '1.0.0'
  apiVersion: '1'

http:
  port: 8080

logger:
  logLevel: 'debug'

redis:
  redisHost: 'redis'
  redisPort: 6379

capacity:
  n: 10
  m: 100
  k: 1000