app:
  name: 'antibruteforce'
  version: '1.0.0'
  apiVersion: '1'

http:
  port: 8080

logger:
  logLevel: 'debug'

redis:
  redisHost: 'redis'
  redisPort: 6379

capacity:
  loginCapacity: 10
  pwdCapacity: 100
  ipCapacity: 1000