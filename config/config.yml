app:
  name: 'antibruteforce'
  version: '1.0.0'
  apiVersion: '1'

http:
  host: 'localhost'
  port: 8080

logger:
  logLevel: 'debug'

redis:
  redisHost: 'localhost'
  redisPort: 6379

capacity:
  N: 10
  M: 100
  K: 1000